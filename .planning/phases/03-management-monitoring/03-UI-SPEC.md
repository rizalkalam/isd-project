---
phase: 3
slug: management-monitoring
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-11
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual Tailwind + hand-rolled components |
| Preset | not applicable |
| Component library | none (custom components in `frontend/src/components/ui/`) |
| Icon library | lucide-react ^0.395.0 |
| Font | Inter, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif |

Source: `frontend/src/index.css`, `frontend/package.json`

No `components.json` found. shadcn is NOT in use. The project uses a manual Tailwind utility approach with two hand-rolled primitives (`card.tsx`, `badge.tsx`). No initialization required.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge inner padding |
| sm | 8px | Compact element spacing, gap between badge and text |
| md | 16px | Default element spacing, table cell padding (px-4) |
| lg | 24px | Section padding, card padding (p-6), stat-card padding (24px) |
| xl | 32px | Layout gaps, page padding, header margin-bottom |
| 2xl | 48px | Major section breaks, empty state vertical padding |
| 3xl | 64px | Page-level spacing (reserved, not currently used in new pages) |

Exceptions:
- `.page-dashboard` top padding: mapped to 32px (`pt-8`) — the existing index.css rule uses 40px but new Dashboard.tsx uses `pt-8` inline Tailwind override to stay on the standard scale.
- Container max-width: `max-w-6xl` (same as existing Loans.tsx). Dashboard uses same constraint.

No non-standard spacing exceptions remain. The former 18px stats-grid gap is replaced with 16px (`gap-4`). The 40px page-dashboard top padding is replaced with 32px (`pt-8`).

Source: `frontend/src/index.css`, `frontend/src/pages/admin/Loans.tsx`, `frontend/src/pages/admin/Inventory.tsx`

---

## Typography

| Role | Size | Weight | Line Height | Tailwind |
|------|------|--------|-------------|---------|
| Label / Caption | 12px (text-xs) | 600 (semibold) | 1.4 | `text-xs font-semibold` |
| Body | 14px (text-sm) | 400 (regular) | 1.5 | `text-sm` |
| Subheading / Metric number | 20px (text-xl) | 600 (semibold) | 1.4 | `text-xl font-semibold` |
| Heading (page h1) | 30px (text-3xl) | 600 (semibold) | 1.2 | `text-3xl font-semibold` |

Declared sizes: 12px, 14px, 20px, 30px (4 total — maximum).
Declared weights: 400 (regular), 600 (semibold) (2 total — maximum).

Notes:
- Metric card large number: use `text-xl font-semibold` (20px, weight 600). The `CardTitle` default of `text-2xl` is overridden here to stay within the 4-size constraint.
- Stat value class `.stat-value` in index.css uses 2.25rem / 800 weight — existing global CSS is not changed, but new Dashboard metric cards use `text-xl font-semibold` via inline Tailwind.
- Table column headers: `text-sm font-semibold text-slate-600` (weight 600, not medium 500).
- Page subtitle / description: `text-sm text-slate-500` (14px, weight 400).

Source: `frontend/src/components/ui/card.tsx`, `frontend/src/pages/admin/Loans.tsx`, `frontend/src/index.css`

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f8fafc` / `#eef2ff` gradient | Page background (body gradient from index.css) |
| Secondary (30%) | `#ffffff` | Cards, table surfaces, metric card backgrounds |
| Accent (10%) | `#6366f1` (indigo-500) / `#4338ca` (indigo-700) | Accent elements listed below |
| Destructive | `#ef4444` (red-500) | Reject button border/text, destructive badge variant |
| Warning / Overdue | `#f59e0b` (amber-500) | Overdue row highlight background, overdue badge |

Accent reserved for:
1. Focus rings on form inputs (`ring-2 ring-blue-500` on search input — existing pattern)
2. `.eyebrow` label text color (`#6366f1` in index.css)
3. Loader border-top color (`#4338ca` in index.css)
4. `.auth-chip` background tint and text — auth pages only
5. New: overdue row highlight background uses `bg-amber-50` with `border-l-4 border-amber-400` — NOT indigo accent

Status badge color map (extends existing `STATUS_BADGE_VARIANT` in Loans.tsx):

| Status | Badge Variant | Background | Text |
|--------|--------------|------------|------|
| PENDING | `secondary` | `bg-slate-100` | `text-slate-900` |
| APPROVED | `default` | `bg-slate-900` | `text-slate-50` |
| ACTIVE | `default` | `bg-slate-900` | `text-slate-50` |
| RETURNED | `outline` | transparent | `text-slate-950` |
| REJECTED | `destructive` | `bg-red-500` | `text-slate-50` |
| OVERDUE | new: `warning` | `bg-amber-500` | `text-slate-50` |

The `warning` variant must be added to `badge.tsx`:
```
warning: 'bg-amber-500 text-slate-50 hover:bg-amber-500/80',
```

Overdue row highlight (table row level, NOT badge level):
- Apply `className="... bg-amber-50 border-l-4 border-amber-400"` to `<tr>` when `isOverdue === true`.
- This is IN ADDITION to the OVERDUE badge on the status cell.

Source: `frontend/src/index.css`, `frontend/src/components/ui/badge.tsx`, `frontend/src/pages/admin/Loans.tsx`, CONTEXT.md D-03

---

## Component Inventory

### New Components (Phase 3)

#### 1. `frontend/src/pages/admin/Dashboard.tsx`
Route: `/admin/dashboard`
Role: Librarian dashboard — metrics overview + filtered loans table.

Layout structure:
```
<div class="container mx-auto p-6 max-w-6xl">
  <header class="mb-8">                     ← page header (h1 + subtitle)
  <section class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">  ← 4 metric cards (gap-4 = 16px)
  <section>                                  ← loans table with filter tabs
    <div class="flex gap-2 mb-4">           ← filter button row (ALL/PENDING/ACTIVE/OVERDUE)
    <div class="rounded-xl border overflow-hidden">  ← table container
```

Metric cards (4 total) — reuse `Card`, `CardHeader`, `CardTitle`, `CardDescription` from `card.tsx`:

| Card | Metric | Icon (lucide-react) |
|------|--------|---------------------|
| Active Loans | Count of `status=ACTIVE` loans | `BookOpen` |
| Pending Requests | Count of `status=PENDING` loans | `Clock` |
| Overdue Items | Count of overdue loans | `AlertTriangle` |
| Total Loans | All-time loan count | `Library` |

Overdue Items card: `CardTitle` text uses `text-amber-600` when count > 0, `text-slate-950` when 0.

Filter tabs (status filter):
- Render as a row of `<button>` elements: ALL | PENDING | ACTIVE | OVERDUE
- Active filter button: `bg-slate-900 text-white` rounded-md px-3 py-1 text-sm font-medium
- Inactive filter button: `border border-slate-200 text-slate-600` same sizing, hover: `hover:bg-slate-50`

Table columns (same structure as `Loans.tsx` — extend, do not duplicate):
Student | Book Title | Status | Due Date | Actions

- Due Date cell: display `—` when `due_date` is null (PENDING/APPROVED rows).
- Overdue row: add `bg-amber-50` background + `border-l-4 border-amber-400` left accent on the `<tr>`.
- Actions column: reuse same `Approve` / `Mark Active` / `Mark Returned` / `Reject` button pattern from `Loans.tsx`. No new action types on Dashboard.

Loading state: Same spinner pattern as `Loans.tsx`:
```jsx
<div className="flex items-center justify-center min-h-[400px]">
  <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-slate-900" />
</div>
```

Empty state (no loans at all):
```jsx
<div className="text-center py-20 bg-slate-50 rounded-2xl border border-dashed">
  <p className="text-slate-500">No loans yet. Approved requests will appear here.</p>
</div>
```

Empty state (filter active, no matching rows):
```jsx
<div className="text-center py-12 bg-slate-50 rounded-xl border border-dashed">
  <p className="text-slate-500">No {filterLabel} loans found.</p>
</div>
```

---

#### 2. `frontend/src/pages/student/Loans.tsx`
Route: `/student/loans`
Role: Student's personal loan history and current status.

Layout structure:
```
<div class="container mx-auto p-6 max-w-4xl">
  <header class="mb-8">                        ← h1 "My Loans" + subtitle
  <div class="space-y-3">                      ← loan rows (no table, card-list style)
```

Each loan row — render as a `<div>` with horizontal layout (flex), NOT a full table:
```
[Book Title / Author]   [Status Badge]   [Due Date or Request Date]
```

- Book Title: `font-medium text-slate-900`, Book Author: `text-xs text-slate-500`
- Status badge: reuse `Badge` with the variant map above (including new `warning` for OVERDUE)
- Due Date: `text-sm text-slate-600`. Show `Due: {date}` when `due_date` is set. Show `—` when null.
- Overdue item: Entire row gets `bg-amber-50 border border-amber-200 rounded-lg` instead of default `bg-white border border-slate-100 rounded-lg`

Loading state: Skeleton placeholder rows (3 items):
```jsx
<div className="space-y-3 animate-pulse">
  {[...Array(3)].map((_, i) => (
    <div key={i} className="h-16 bg-slate-100 rounded-lg" />
  ))}
</div>
```

Empty state:
```jsx
<div className="text-center py-20 bg-slate-50 rounded-2xl border border-dashed">
  <p className="text-slate-500">You have no loan history yet.</p>
  <a href="/student/discovery" className="mt-4 inline-block text-sm text-indigo-600 hover:underline">
    Browse the catalog to request a book
  </a>
</div>
```

---

### Modified Components (Phase 3)

#### `frontend/src/components/ui/badge.tsx`
Add `warning` variant to the `variants` map:
```ts
warning: 'bg-amber-500 text-slate-50 hover:bg-amber-500/80',
```
Update `BadgeProps` interface variant union:
```ts
variant?: 'default' | 'secondary' | 'destructive' | 'outline' | 'success' | 'warning';
```

#### `frontend/src/App.tsx`
Add two new routes:
```tsx
<Route path="/admin/dashboard" element={<Dashboard />} />
<Route path="/student/loans" element={<StudentLoans />} />
```

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Dashboard page heading | `Library Dashboard` |
| Dashboard page subtitle | `Monitor loans, requests, and overdue items across the library.` |
| Student loans page heading | `My Loans` |
| Student loans page subtitle | `Track your current and past borrow requests.` |
| Metric card: Active Loans | Label: `Active Loans` |
| Metric card: Pending Requests | Label: `Pending Requests` |
| Metric card: Overdue Items | Label: `Overdue Items` |
| Metric card: Total Loans | Label: `Total Loans` |
| Filter tab: all loans | `All` |
| Filter tab: pending | `Pending` |
| Filter tab: active | `Active` |
| Filter tab: overdue | `Overdue` |
| Due date column header | `Due Date` |
| Due date cell (null) | `—` |
| Due date cell (set) | `{formatted date}` — use `toLocaleDateString()` |
| Overdue row tooltip / aria-label | `This loan is overdue` |
| Dashboard empty state | `No loans yet. Approved requests will appear here.` |
| Dashboard filter empty state | `No {filter} loans found.` — lowercase filter label |
| Student loans empty state | `You have no loan history yet.` + link `Browse the catalog to request a book` |
| Loading spinner aria-label | `Loading loans…` |
| Error state | `Could not load loans. Please refresh the page or contact support if the problem persists.` |
| No destructive actions in this phase | — (no deletes, no confirmations required) |

Source: CONTEXT.md decisions D-01 through D-06; existing copy patterns from Loans.tsx and Discovery.tsx.

---

## Interaction Contract

### Dashboard filter tabs
- Default selected filter on page load: `ALL`
- Filter is client-side only — all loans are fetched once; the active filter value controls which rows render.
- Selected tab gets `bg-slate-900 text-white`; deselected tabs get `border border-slate-200 text-slate-600`.
- Tab switch is immediate (no loading state — data is already in memory).
- Keyboard: tabs are `<button>` elements, naturally focusable. No custom keyboard handling required.

### Overdue detection
- Computed client-side after data fetch: a loan is overdue when `status === 'ACTIVE' && due_date && new Date(due_date) < new Date()`.
- The `/api/v1/loans/overdue` endpoint is the authoritative source for the dashboard's Overdue Items metric card count.
- The dashboard table applies the overdue row style using this same client-side check on the full loans list.

### Metric card loading
- Cards render their count as `—` (em dash) while the stats fetch is in flight.
- No skeleton/shimmer on cards — the em dash placeholder is sufficient for MVP.

### Student loans page
- Sorted by `created_at` descending (most recent first).
- No pagination for MVP — render all loans in a single list.
- No filter controls — student sees all their own loans.

### Transition / animation
- Row hover: `hover:bg-slate-50 transition-colors` (same as Loans.tsx).
- Overdue row hover: `hover:bg-amber-100 transition-colors` (slightly deeper amber on hover).
- No other animations beyond existing spinner pattern.

### Responsive breakpoints
- Dashboard metric cards: `grid-cols-2 md:grid-cols-4` — 2 columns on mobile, 4 on md+.
- Dashboard table: horizontal scroll on small screens — wrap table in `overflow-x-auto`.
- Student loans list: single column at all breakpoints (stacked flex rows, no grid).
- Container: `container mx-auto p-6 max-w-6xl` for dashboard; `max-w-4xl` for student loans page.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party registries declared. All components are hand-rolled or reuse existing codebase primitives.

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|---------------|
| CONTEXT.md | 6 (D-01 through D-06: card count, table columns, overdue highlight, due_date logic, on-query detection, no notification feed) |
| RESEARCH.md | not present for this phase |
| components.json | not present — shadcn not in use |
| `card.tsx` (codebase) | Card spacing (p-6), CardTitle size (text-2xl semibold), CardDescription style |
| `badge.tsx` (codebase) | All 5 existing variants; `warning` variant added |
| `Loans.tsx` (codebase) | Table structure, STATUS_BADGE_VARIANT map, button styles, loading/error/empty patterns |
| `Discovery.tsx` (codebase) | Page container pattern, skeleton loading, feedback banner pattern |
| `index.css` (codebase) | Font family, body background gradient, accent color (#6366f1), spacing tokens (.page-dashboard, .stats-grid, .stat-card) |
| User input | 0 — all decisions pre-populated from upstream artifacts and codebase scan |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
