---
phase: 01-foundation-catalog
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/models/title.py
  - backend/app/models/copy.py
  - backend/app/api/v1/endpoints/titles.py
  - frontend/src/features/catalog/components/BookCard.tsx
  - frontend/src/pages/admin/Inventory.tsx
  - frontend/src/components/modals/BookForm.tsx
autonomous: false
requirements: [CAT-01, CAT-02]

must_haves:
  truths:
    - "Librarian can create, update, and delete book titles"
    - "Librarian can add physical copies to a specific book title"
    - "Book titles are displayed as cards in a visual gallery"
    - "Inventory status (available/unavailable) is tracked per physical copy"
  artifacts:
    - path: "backend/app/models/title.py"
      provides: "Title model (Metadata)"
    - path: "backend/app/models/copy.py"
      provides: "Copy model (Physical inventory)"
    - path: "frontend/src/features/catalog/components/BookCard.tsx"
      provides: "Visual representation of book titles"
  key_links:
    - from: "backend/app/models/copy.py"
      to: "backend/app/models/title.py"
      via: "Relationship(back_populates='copies')"
    - from: "frontend/src/pages/admin/Inventory.tsx"
      to: "backend/app/api/v1/endpoints/titles.py"
      via: "React Query useQuery('/titles')"
---

<objective>
Implement the core catalog and inventory management system, separating logical book metadata from physical book copies.

**As a** librarian, **I want to** manage book titles and their physical copies, **so that** the inventory is accurate and ready for borrowing.

Purpose: Build the "Source of Truth" for the library collection.
Output: Visual management interface for book titles and their physical inventory.
</objective>

<execution_context>
@C:/Users/ACER/OneDrive - Universitas Islam Indonesia/Documents/psi/isd-project/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/ACER/OneDrive - Universitas Islam Indonesia/Documents/psi/isd-project/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-catalog/01-CONTEXT.md
@.planning/phases/01-foundation-catalog/01-PATTERNS.md
@.planning/phases/01-foundation-catalog/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catalog & Inventory Models/API</name>
  <files>backend/app/models/title.py, backend/app/models/copy.py, backend/app/api/v1/endpoints/titles.py</files>
  <read_first>.planning/phases/01-foundation-catalog/01-PATTERNS.md (Backend - Models)</read_first>
  <behavior>
    - GET /titles: Returns list of titles with copy counts.
    - POST /titles: Creates a new title (metadata).
    - POST /titles/{id}/copies: Adds a physical copy to a title.
    - Relationship: Ensure deleting a title handles associated copies (cascade or protect).
  </behavior>
  <action>
    - Create `backend/app/models/title.py` (id, title, author, isbn, cover_url).
    - Create `backend/app/models/copy.py` (id, title_id, status: Enum[AVAILABLE, BORROWED, LOST], barcode).
    - Implement CRUD endpoints in `titles.py` with `LIBRARIAN` role protection.
  </action>
  <verify>
    <automated>pytest backend/app/tests/test_catalog.py</automated>
  </verify>
  <done>Backend catalog models and API are ready for physical inventory tracking.</done>
  <acceptance_criteria>
    - Titles and Copies are separate tables in DB.
    - API correctly implements the relationship (One Title -> Many Copies).
    - Only users with 'LIBRARIAN' role can modify the catalog.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Visual Catalog Gallery UI</name>
  <files>frontend/src/features/catalog/components/BookCard.tsx, frontend/src/pages/admin/Inventory.tsx</files>
  <read_first>.planning/phases/01-foundation-catalog/01-PATTERNS.md (Frontend - Component)</read_first>
  <action>
    - Implement `BookCard.tsx` using shadcn/ui Card with title, author, and cover image.
    - Create `Inventory.tsx` page that fetches titles using TanStack Query.
    - Use a grid layout for the card gallery.
  </action>
  <verify>
    <automated>npm test frontend/src/features/catalog/components/BookCard.test.tsx</automated>
  </verify>
  <done>Librarians can view the book collection in a modern visual card gallery.</done>
  <acceptance_criteria>
    - Book cards display title, author, and cover image.
    - Inventory page correctly renders the grid of cards fetched from the API.
    - Loading and Empty states are handled.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Physical Copy Management flow</name>
  <files>frontend/src/components/modals/BookForm.tsx</files>
  <read_first>.planning/phases/01-foundation-catalog/01-CONTEXT.md (Management UI)</read_first>
  <what-built>Interface for adding new titles and managing individual physical copies.</what-built>
  <how-to-verify>
    1. Log in as a Librarian.
    2. Go to the Inventory page.
    3. Click "Add New Book" and fill in title/author/ISBN/Cover URL.
    4. Click on a book card to see its copies.
    5. Add 2 physical copies to that book.
    6. Verify that the copies appear in the list with "AVAILABLE" status.
  </how-to-verify>
  <resume-signal>Type "approved" if you can successfully manage titles and physical copies end-to-end.</resume-signal>
  <done>Librarians can fully manage the library's physical inventory through the UI.</done>
  <acceptance_criteria>
    - BookForm modal handles both Title creation and Copy addition.
    - Inventory UI updates immediately after successful creation (cache invalidation).
    - Copy status is clearly visible (e.g., Badge component).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Librarian → Inventory API | Validated CRUD operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Book ISBN | mitigate | Validate ISBN format using Pydantic/SQLModel validator. |
| T-01-05 | Information Disclosure | Metadata | accept | Book metadata is public information for the catalog. |
| T-01-06 | Elevation of Privilege | CRUD Access | mitigate | Enforce `LIBRARIAN` role dependency on all write endpoints. |
</threat_model>

<verification>
- `curl -H "Authorization: Bearer <TOKEN>" http://localhost:8000/api/v1/titles` returns title list.
- React Inventory page displays cards with correct metadata.
- Database contains matching records in `titles` and `copies` tables.
</verification>

<success_criteria>
- Librarian can perform full CRUD on titles.
- Multiple physical copies can be tracked for a single title.
- Requirement IDs [CAT-01, CAT-02] are fully addressed.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-catalog/01-02-SUMMARY.md` when done
</output>

<artifacts_this_phase_produces>
- `backend/app/models/title.py`: `Title` SQLModel.
- `backend/app/models/copy.py`: `Copy` SQLModel.
- `backend/app/api/v1/endpoints/titles.py`: CRUD router for titles/copies.
- `frontend/src/features/catalog/components/BookCard.tsx`: Reusable book UI component.
- `frontend/src/pages/admin/Inventory.tsx`: Librarian inventory dashboard.
</artifacts_this_phase_produces>
