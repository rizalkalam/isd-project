---
phase: 01-foundation-catalog
plan: 03
type: execute
wave: 3
gap_closure: true
depends_on: ["01-02"]
files_modified: [
  "backend/app/models/title.py",
  "backend/app/api/v1/endpoints/titles.py",
  "frontend/src/components/modals/BookForm.tsx"
]
autonomous: true
requirements: ["CAT-01"]

must_haves:
  truths:
    - "Librarian can update existing book title details (title, author, ISBN, cover URL)"
    - "Librarian can delete a book title, which also removes its associated physical copies"
  artifacts:
    - path: "backend/app/models/title.py"
      provides: "TitleUpdate schema"
    - path: "backend/app/api/v1/endpoints/titles.py"
      provides: "PUT and DELETE endpoints for titles"
    - path: "frontend/src/components/modals/BookForm.tsx"
      provides: "UI for updating and deleting titles"
  key_links:
    - from: "BookForm.tsx"
      to: "PUT /api/v1/titles/{id}"
      via: "updateTitleMutation"
    - from: "BookForm.tsx"
      to: "DELETE /api/v1/titles/{id}"
      via: "deleteTitleMutation"
---

<objective>
Complete the Catalog CRUD (CAT-01) by implementing missing Update and Delete functionality for book titles. This addresses the gaps identified in Phase 01 Verification.

Purpose: Allow librarians to correct metadata errors and remove titles from the catalog.
Output: Functional PUT and DELETE API endpoints and integrated UI actions.
</objective>

<execution_context>
@C:/Users/ACER/OneDrive - Universitas Islam Indonesia/Documents/psi/isd-project/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/ACER/OneDrive - Universitas Islam Indonesia/Documents/psi/isd-project/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-catalog/01-VERIFICATION.md
@.planning/phases/01-foundation-catalog/01-02-SUMMARY.md
@backend/app/models/title.py
@backend/app/api/v1/endpoints/titles.py
@frontend/src/components/modals/BookForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Title CRUD Completion</name>
  <files>backend/app/models/title.py, backend/app/api/v1/endpoints/titles.py</files>
  <read_first>backend/app/api/v1/endpoints/titles.py</read_first>
  <action>
    - In `backend/app/models/title.py`, add `TitleUpdate(SQLModel)` class where all fields (title, author, isbn, cover_url) are `Optional` and default to `None`.
    - In `backend/app/api/v1/endpoints/titles.py`, implement `update_title` (`PUT /{title_id}`):
        - Protected by `LIBRARIAN` role.
        - Fetch existing title by ID or raise 404.
        - If ISBN is updated, verify the new ISBN doesn't conflict with another record.
        - Update fields using `title_in.dict(exclude_unset=True)`.
        - Commit and return updated title.
    - In `backend/app/api/v1/endpoints/titles.py`, implement `delete_title` (`DELETE /{title_id}`):
        - Protected by `LIBRARIAN` role.
        - Fetch and delete title (ensure cascade delete for copies is handled by DB/ORM).
        - Return 204 No Content.
  </action>
  <acceptance_criteria>
    - `PUT /titles/{id}` returns 200 with updated fields.
    - `PUT /titles/{id}` returns 400 if updating to an existing ISBN.
    - `DELETE /titles/{id}` returns 204 and title is removed from DB.
  </acceptance_criteria>
  <verify>
    <automated>
      # verify endpoints exist and return 401/403 for unauthorized users
      curl -X PUT http://localhost:8000/api/v1/titles/1 -o /dev/null -w "%{http_code}" | grep -E "401|403|404"
    </automated>
  </verify>
  <done>Update and Delete endpoints are implemented and protected.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Management UI Integration</name>
  <files>frontend/src/components/modals/BookForm.tsx</files>
  <read_first>frontend/src/components/modals/BookForm.tsx</read_first>
  <action>
    - Add `updateTitleMutation` using `api.put("/titles/${selectedTitle.id}", data)`.
    - Add `deleteTitleMutation` using `api.delete("/titles/${selectedTitle.id}")`.
    - Update `handleSubmit` to call `updateTitleMutation` if `selectedTitle` exists.
    - Modify the form UI:
        - When `selectedTitle` exists, display the metadata form (currently it's hidden behind the "Copies" management view).
        - Use a Tabbed view or stacked sections to allow editing metadata AND managing copies simultaneously when in "Edit" mode.
        - Add a "Delete Title" button in the footer or top-right when `selectedTitle` exists, with a `window.confirm` guard.
    - Ensure `queryClient.invalidateQueries({ queryKey: ["titles"] })` is called on both mutations.
  </action>
  <acceptance_criteria>
    - Editing a title in the modal and saving updates the catalog view.
    - Deleting a title in the modal removes it from the catalog view and closes the modal.
  </acceptance_criteria>
  <verify>
    <automated>grep -E "updateTitleMutation|deleteTitleMutation" frontend/src/components/modals/BookForm.tsx</automated>
  </verify>
  <done>UI for updating and deleting titles is functional in the BookForm modal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Librarian → API | Authorized management operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | `PUT /titles/{id}` | mitigate | Validate inputs with SQLModel schemas; enforce `LIBRARIAN` role dependency. |
| T-01-03-02 | Information Disclosure | `DELETE /titles/{id}` | mitigate | Ensure only own library's data is deletable (future multi-tenant), for now enforce `LIBRARIAN` role. |
| T-01-03-SC | Tampering | npm installs | accept | No new packages added in this plan. |
</threat_model>

<verification>
### Overall Phase Checks
- [ ] Librarian can update a book's ISBN and it's reflected in the database.
- [ ] Librarian can delete a book and its physical copies are also gone from the copies table.
</verification>

<success_criteria>
- All CRUD operations for Book Titles (CAT-01) are verified via API tests or manual walkthrough.
- 01-VERIFICATION.md gap for "Librarian can create, update, and delete book titles" can be marked as VERIFIED.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-catalog/01-03-SUMMARY.md` when done
</output>
