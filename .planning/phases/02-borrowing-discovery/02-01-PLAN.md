---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/endpoints/titles.py
  - backend/tests/api/test_titles_search.py
  - frontend/src/features/catalog/api/search.ts
  - frontend/src/pages/student/Discovery.tsx
autonomous: true
requirements:
  - CAT-03
  - CAT-04
must_haves:
  truths:
    - "Student can search the catalog by title, author, or ISBN."
    - "Student can see real-time availability for each book."
  artifacts:
    - path: "backend/app/api/v1/endpoints/titles.py"
      provides: "Search API endpoint with dynamic availability"
    - path: "frontend/src/pages/student/Discovery.tsx"
      provides: "Search UI for students"
  key_links:
    - from: "frontend/src/pages/student/Discovery.tsx"
      to: "/api/v1/titles/search"
      via: "API call"
---

## Phase Goal

**As a** student, **I want to** search the catalog and see real-time availability, **so that** I can find books to borrow.

<objective>
Implement search and dynamic availability for the catalog.
Purpose: Allow students to discover books and know if they are available.
Output: Search API and Student Discovery UI.
</objective>

<execution_context>
@.planning/phases/02-borrowing-discovery/02-CONTEXT.md
@.planning/phases/02-borrowing-discovery/02-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E Test for Search and Availability</name>
  <read_first>backend/tests/api/test_titles.py</read_first>
  <files>backend/tests/api/test_titles_search.py</files>
  <behavior>
    - Test 1: Search by title returns matching books.
    - Test 2: Search by author returns matching books.
    - Test 3: Search response includes dynamic availability count.
  </behavior>
  <action>Create failing E2E tests for the `GET /api/v1/titles/search` endpoint. Ensure the tests check for SQL ILIKE search capability and that the availability count is dynamically calculated based on total copies minus active loans.</action>
  <verify>
    <automated>pytest backend/tests/api/test_titles_search.py</automated>
  </verify>
  <acceptance_criteria>Tests are written, executable, and currently fail.</acceptance_criteria>
  <done>Failing tests exist for the search endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Search API</name>
  <read_first>backend/app/api/v1/endpoints/titles.py, backend/app/models/title.py, backend/app/models/copy.py</read_first>
  <files>backend/app/api/v1/endpoints/titles.py, backend/app/schemas/title.py</files>
  <action>Add a new schema `TitleWithAvailability` that extends `Title` with an `available_copies` integer. Create `GET /api/v1/titles/search` endpoint using SQLModel. Implement SQL `ILIKE` for searching `title`, `author`, or `isbn`. Calculate `available_copies` dynamically using a subquery or by counting `copies` and subtracting active loans (if loans exist, or just total copies for now). Per 02-CONTEXT.md: use standard SQL ILIKE queries and dynamic calculation.</action>
  <verify>
    <automated>pytest backend/tests/api/test_titles_search.py</automated>
  </verify>
  <acceptance_criteria>Endpoint returns books matching search query with available copies count. Tests pass.</acceptance_criteria>
  <done>API successfully handles search queries and passes tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Student Discovery UI</name>
  <read_first>frontend/src/features/catalog/components/BookCard.tsx, frontend/src/pages/admin/Inventory.tsx</read_first>
  <files>frontend/src/pages/student/Discovery.tsx, frontend/src/features/catalog/api/search.ts</files>
  <action>Create API utility for calling `/titles/search`. Implement the `Discovery` page for students with a search bar and a grid of `BookCard`s. Display the `available_copies` dynamically on each card using `frontend/src/components/ui/badge.tsx`. Connect the search input state to the React Query hook to fetch results as the user types or submits.</action>
  <verify>
    <automated>npm run test -- --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>UI fetches from search endpoint and displays books with availability badges.</acceptance_criteria>
  <done>Discovery page is functional and connected to the API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted search queries cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `/titles/search` | mitigate | Sanitize search inputs and use parameterized queries via SQLModel to prevent SQL injection. |
</threat_model>
