---
phase: 02
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - backend/app/api/v1/endpoints/loans.py
  - backend/tests/api/test_loans_admin.py
  - frontend/src/pages/admin/Loans.tsx
  - frontend/src/features/loans/api/loans.ts
  - frontend/src/App.tsx
autonomous: true
requirements:
  - CIRC-02
  - CIRC-03
must_haves:
  truths:
    - "Librarian can approve or reject pending borrow requests."
    - "Librarian can record the return of an active loan."
  artifacts:
    - path: "backend/app/api/v1/endpoints/loans.py"
      provides: "Loan status update endpoints"
    - path: "frontend/src/pages/admin/Loans.tsx"
      provides: "Librarian interface for managing loans"
  key_links:
    - from: "frontend/src/pages/admin/Loans.tsx"
      to: "/api/v1/loans/{id}/status"
      via: "API PUT request"
---

## Phase Goal

**As a** librarian, **I want to** approve requests and record returns, **so that** the library's physical inventory remains accurately tracked.

<objective>
Implement librarian endpoints and UI to manage the loan lifecycle.
Purpose: Allow librarians to fulfill student requests and handle book returns.
Output: Loan status update APIs and Librarian Loans management page.
</objective>

<execution_context>
@.planning/phases/02-borrowing-discovery/02-CONTEXT.md
@.planning/phases/02-borrowing-discovery/02-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E Test for Librarian Fulfill & Return</name>
  <read_first>backend/tests/api/test_loans_borrow.py</read_first>
  <files>backend/tests/api/test_loans_admin.py</files>
  <behavior>
    - Test 1: Librarian can change a PENDING loan to APPROVED.
    - Test 2: Librarian can change a PENDING loan to REJECTED.
    - Test 3: Librarian can change an ACTIVE loan to RETURNED.
    - Test 4: Dynamic availability updates instantly upon RETURNED or REJECTED state change.
  </behavior>
  <action>Create failing E2E tests for `PUT /api/v1/loans/{id}/status`. Validate that only Librarians can access these endpoints and that the state transitions adhere to the Loan lifecycle.</action>
  <verify>
    <automated>pytest backend/tests/api/test_loans_admin.py</automated>
  </verify>
  <acceptance_criteria>Tests for loan status updates exist and fail.</acceptance_criteria>
  <done>Failing tests for librarian fulfillment are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Loan Status API</name>
  <read_first>backend/app/api/v1/endpoints/loans.py, backend/app/models/loan.py</read_first>
  <files>backend/app/api/v1/endpoints/loans.py</files>
  <action>Add `GET /api/v1/loans` (Librarian only) to list all loans. Add `PUT /api/v1/loans/{id}/status` to update loan state. Ensure transitions follow the state machine (e.g., PENDING -> APPROVED/REJECTED, APPROVED -> ACTIVE, ACTIVE -> RETURNED). Ensure the endpoint requires Librarian role.</action>
  <verify>
    <automated>pytest backend/tests/api/test_loans_admin.py</automated>
  </verify>
  <acceptance_criteria>API correctly updates loan status and enforces role authorization.</acceptance_criteria>
  <done>Loan status update endpoints are fully functional.</done>
</task>

<task type="auto">
  <name>Task 3: Librarian Loans UI</name>
  <read_first>frontend/src/features/loans/api/loans.ts, frontend/src/pages/admin/Inventory.tsx</read_first>
  <files>frontend/src/pages/admin/Loans.tsx, frontend/src/features/loans/api/loans.ts, frontend/src/App.tsx</files>
  <action>Create API utilities for fetching all loans and updating loan status. Create `Loans.tsx` page for librarians displaying a table or list of loans with their current status. Add action buttons for "Approve", "Reject", "Mark Active", and "Mark Returned" based on the loan's current state. Register the route in `App.tsx`.</action>
  <verify>
    <automated>npm run test -- --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>Librarians can view all loans and update their statuses through the UI.</acceptance_criteria>
  <done>Librarian Loans management page is built and connected to APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Librarian -> API | Loan state updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | `/loans/{id}/status` | mitigate | Implement strict role checks (`check_role(UserRole.LIBRARIAN)`) on status update endpoints. |
</threat_model>
