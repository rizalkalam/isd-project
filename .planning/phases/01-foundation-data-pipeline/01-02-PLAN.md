---
phase: 01-foundation-data-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/services/anonymization.ts
  - src/api/ingestion.ts
  - prisma/schema.prisma
  - .env
autonomous: true
requirements: [EPI-05]
user_setup:
  - service: Encryption
    why: "Hashing patient IDs"
    env_vars:
      - name: HASH_PEPPER
        source: "Secret string for system-wide hashing pepper"

must_haves:
  truths:
    - "Stored patient records contain no Nama or NIK fields"
    - "id_pasien is hashed using SHA-256 with salt and pepper"
    - "Date of birth is stored only as Year of Birth"
    - "Aggregations on clinical data are optimized via B-Tree indices"
  artifacts:
    - path: "src/services/anonymization.ts"
      provides: "PII stripping and ID hashing logic"
    - path: "prisma/schema.prisma"
      provides: "B-Tree indices on clinical fields"
  key_links:
    - from: "src/api/ingestion.ts"
    - to: "src/services/anonymization.ts"
      via: "Function call before DB write"
---

<objective>
Implement the core data privacy and performance requirements. This includes the [LOCKED] decisions for PII redaction (Nama/NIK), deterministic patient ID pseudonymization (SHA-256 hashing), and data roll-up (DOB to Year), fulfilling EPI-05. It also optimizes the database for future analytical queries via B-Tree indexing.

Purpose: Ensure data privacy compliance and query performance.
Output: Anonymized data pipeline and optimized database schema.
</objective>

<execution_context>
@C:/Users/ACER/OneDrive - Universitas Islam Indonesia/Documents/psi/isd-project/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/ACER/OneDrive - Universitas Islam Indonesia/Documents/psi/isd-project/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-data-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-data-pipeline/01-RESEARCH.md
@.planning/phases/01-foundation-data-pipeline/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Anonymization Service</name>
  <files>src/services/anonymization.ts, .env</files>
  <read_first>src/services/anonymization.ts, .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md</read_first>
  <action>
    Create `src/services/anonymization.ts` to implement the [LOCKED] privacy logic:
    1. `hashPatientId(id, salt)`: Uses Node.js `crypto` to create a SHA-256 hash of `id + salt + HASH_PEPPER`.
    2. `redactPII(payload)`: Returns a new object with `nama` and `nik` removed.
    3. `extractYear(dob)`: Converts ISO 8601 string to a 4-digit Integer year.
    Add `HASH_PEPPER` to `.env`.
  </action>
  <verify>
    <automated>npx ts-node -e "import { hashPatientId } from './src/services/anonymization'; console.log(hashPatientId('test', 'salt'))" | grep -E '^[a-f0-9]{64}$'</automated>
  </verify>
  <acceptance_criteria>
    - Service correctly hashes IDs to 64-character hex strings (SHA-256).
    - Redaction logic successfully removes `nama` and `nik` fields.
    - DOB transformation correctly extracts the year.
  </acceptance_criteria>
  <done>Anonymization logic implemented as a reusable service.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Anonymization into Ingestion Pipeline</name>
  <files>src/api/ingestion.ts</files>
  <read_first>src/api/ingestion.ts, src/services/anonymization.ts</read_first>
  <action>
    Modify `src/api/ingestion.ts` to use the anonymization service before persisting data:
    1. Hash the `id_pasien` (use `nik` as the salt for deterministic hashing if appropriate, or a separate provided salt).
    2. Transform `dob` to `tahun_lahir`.
    3. Ensure `nama` and `nik` are NOT passed to the Prisma client for storage.
    Verify that the data stored in the database is fully anonymized.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/ingest ... && (echo "SELECT * FROM Pasien;" | psql $DATABASE_URL)</automated>
  </verify>
  <acceptance_criteria>
    - Database records for `Pasien` contain a hash in `id_pasien_hash` and an integer in `tahun_lahir`.
    - No `nama` or `nik` data exists in any analytical database table.
  </acceptance_criteria>
  <done>Ingestion pipeline now automatically anonymizes clinical data before persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Database Indexing and Security Hardening</name>
  <files>prisma/schema.prisma, src/app.ts</files>
  <read_first>prisma/schema.prisma, .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md</read_first>
  <action>
    Implement performance and security optimizations:
    1. Add B-Tree indices to `Rekam_Medis(tanggal_periksa, kode_diagnosis)` in `prisma/schema.prisma` (per CONTEXT.md Performance decision).
    2. Add `express-rate-limit` to `src/app.ts` to protect the ingestion endpoint.
    3. Run `npx prisma migrate dev` to apply indices.
  </action>
  <verify>
    <automated>npx prisma validate && curl -I http://localhost:3000/api/ingest | grep "X-RateLimit-Limit"</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `@@index` for the specified fields.
    - Rate limiting headers are present in API responses.
    - Indices are applied to the PostgreSQL database.
  </acceptance_criteria>
  <done>Database optimized for analytical queries and API hardened against DoS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | Anonymized data crosses here. PII must never cross this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | Database Storage | mitigate | PII (Name/NIK) redaction in memory; ID hashing with pepper. |
| T-01-04 | Denial of Service | API Ingestion | mitigate | Implement express-rate-limit on all endpoints. |
| T-01-05 | Information Disclosure | Logs | mitigate | Ensure PII is redacted before any console.log or logging middleware. |
</threat_model>

<verification>
- Automated check: Unit tests for `anonymization.ts` pass.
- Manual check: Inspect database rows to confirm no PII is stored.
- Automated check: `npx prisma migrate status` confirms indices are up to date.
</verification>

<success_criteria>
- Name and NIK are never saved to the database.
- Patient IDs are consistently hashed across multiple visits.
- Database queries on diagnosis and date are indexed.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-pipeline/01-02-SUMMARY.md` when done

### Artifacts this phase produces:
- `src/services/anonymization.ts` (Anonymization Service)
- `hashPatientId` (Function)
- `redactPII` (Function)
- `extractYear` (Function)
- `HASH_PEPPER` (Environment Variable)
- B-Tree Indices on `Rekam_Medis` (Database Indices)
</output>
