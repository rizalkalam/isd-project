---
phase: 01-foundation-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - prisma/schema.prisma
  - .env
  - src/app.ts
  - src/api/ingestion.ts
  - src/middleware/auth.ts
  - src/schema/ingestion.ts
autonomous: true
requirements: [EPI-04, SYS-02]
user_setup:
  - service: PostgreSQL
    why: "Analytical data store"
    env_vars:
      - name: DATABASE_URL
        source: "Connection string to local or hosted PostgreSQL instance"

must_haves:
  truths:
    - "API accepts clinical data pushes from external sources"
    - "System stores clinical records in a PostgreSQL analytical database"
    - "The system has no connection strings or access to the legacy TPS database"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Data models for Pasien, Kunjungan, and Rekam_Medis"
    - path: "src/api/ingestion.ts"
      provides: "REST endpoint for data push"
    - path: "src/middleware/auth.ts"
      provides: "API Key authentication"
  key_links:
    - from: "src/api/ingestion.ts"
    - to: "prisma.pasien.upsert"
      via: "Prisma Client"
---

<objective>
Establish the foundational analytical infrastructure including the PostgreSQL database schema and the Express-based ingestion API skeleton. This fulfills the requirement for a decoupled analytical pipeline (EPI-04) that is strictly separated from the legacy TPS (SYS-02).

Purpose: Create the landing zone for clinical data without risking the legacy system's integrity.
Output: Working API server with database persistence and basic authentication.
</objective>

<execution_context>
@C:/Users/ACER/OneDrive - Universitas Islam Indonesia/Documents/psi/isd-project/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/ACER/OneDrive - Universitas Islam Indonesia/Documents/psi/isd-project/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-data-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-data-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization and Database Schema</name>
  <files>package.json, prisma/schema.prisma, .env, src/schema/ingestion.ts</files>
  <read_first>package.json, .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md</read_first>
  <action>
    Initialize the project with Node.js and TypeScript. Install dependencies: express (5.2.1), prisma (7.8.0), zod (4.4.3), and dotenv. 
    Create `prisma/schema.prisma` with models: 
    - `Pasien`: `id_pasien_hash` (String, @id), `tahun_lahir` (Int).
    - `Kunjungan`: `id_kunjungan` (String, @id), `id_pasien_hash` (String), `waktu_check_in` (DateTime), `status` (String).
    - `Rekam_Medis`: `id_rm` (String, @id), `id_kunjungan` (String), `kode_diagnosis` (String), `tanggal_periksa` (DateTime).
    Define Zod schema in `src/schema/ingestion.ts` matching the payload structure defined in RESEARCH.md.
  </action>
  <verify>
    <automated>npx prisma validate && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains express 5.2.1 and prisma 7.8.0.
    - `prisma/schema.prisma` contains the three required models with specified fields (per CONTEXT.md).
    - `src/schema/ingestion.ts` exports a Zod schema validating patient, visit, and medical record fields.
  </acceptance_criteria>
  <done>Infrastructure dependencies installed and database schema validated.</done>
</task>

<task type="auto">
  <name>Task 2: API Skeleton and Authentication Middleware</name>
  <files>src/app.ts, src/middleware/auth.ts</files>
  <read_first>src/app.ts</read_first>
  <action>
    Create an Express app in `src/app.ts` listening on port 3000. 
    Implement `src/middleware/auth.ts` that checks for an `x-api-key` header against an `INGESTION_API_KEY` in `.env`.
    Add `helmet` and `json` body-parser to the middleware stack.
  </action>
  <verify>
    <automated>curl -I -X POST http://localhost:3000/api/ingest -H "x-api-key: INVALID" | grep 401</automated>
  </verify>
  <acceptance_criteria>
    - Server starts and responds with 401 Unauthorized when the API key is missing or incorrect.
    - Server responds with 404 for unknown routes.
    - Middleware correctly extracts and validates the `x-api-key` header.
  </acceptance_criteria>
  <done>Express server running with basic API key protection.</done>
</task>

<task type="auto">
  <name>Task 3: Basic Ingestion Endpoint with Upsert Logic</name>
  <files>src/api/ingestion.ts, src/app.ts</files>
  <read_first>src/schema/ingestion.ts, src/middleware/auth.ts</read_first>
  <action>
    Implement `POST /api/ingest` in `src/api/ingestion.ts`. 
    The route should:
    1. Validate the payload using the Zod schema from Task 1.
    2. Use Prisma to perform `upsert` operations for Pasien, Kunjungan, and Rekam_Medis records to handle duplicates (per CONTEXT.md specifics).
    3. Return 201 Created on success.
    Note: In this plan, use the input IDs directly; hashing and PII redaction will be implemented in Plan 02.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/ingest -H "x-api-key: dev-key" -H "Content-Type: application/json" -d '{"id_pasien":"P1", "nama":"Test", "nik":"123", "dob":"1990-01-01T00:00:00Z", "kunjungan":{"id_kunjungan":"K1", "waktu_check_in":"2023-01-01T10:00:00Z", "status":"Finished"}, "rekam_medis":[{"id_rm":"RM1", "kode_diagnosis":"A00", "tanggal_periksa":"2023-01-01T11:00:00Z"}]}'</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/ingest` validates payload and returns 201 Created for valid data.
    - Data is persisted in PostgreSQL using the Prisma client.
    - Repeated requests with the same IDs do not create duplicate rows (Upsert logic).
  </acceptance_criteria>
  <done>Clinical data can be successfully pushed to the analytical database via API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Puskesmas → API | Untrusted JSON clinical data enters the system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | /api/ingest | mitigate | Implement API Key authentication in middleware. |
| T-01-02 | Tampering | Ingestion Payload | mitigate | Use Zod schema validation to enforce structure and types. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages (express, prisma, zod, helmet). |
</threat_model>

<verification>
- Automated check: `npx prisma validate` passes.
- Automated check: Integration test (curl) returns 201 for valid payload and 401 for unauthorized.
</verification>

<success_criteria>
- Analytical database exists and is reachable.
- REST API endpoint `/api/ingest` is functional.
- Data sent to API is stored in the database without duplicates on re-send.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-pipeline/01-01-SUMMARY.md` when done

### Artifacts this phase produces:
- `prisma/schema.prisma` (Database Schema)
- `src/app.ts` (Express Application)
- `src/api/ingestion.ts` (Ingestion Route)
- `src/middleware/auth.ts` (Authentication Middleware)
- `src/schema/ingestion.ts` (Zod Schema)
- `INGESTION_API_KEY` (Environment Variable)
- `DATABASE_URL` (Environment Variable)
</output>
